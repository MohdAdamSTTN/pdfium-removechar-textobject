[project]
name = "pypdfium2-custom"
version = "0.1.0"
description = "Extra PDFium APIs for pypdfium2"
dependencies = [
    "pypdfium2>=4.29.0,<6"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
